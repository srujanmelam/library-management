import React from 'react'

function SearchBooks() {
    return (
        <div>
            <h1>Search bar with drag-down(contains filters) and pagination</h1>
        </div>
    )
}

export default SearchBooks
